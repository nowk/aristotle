%h1= @user.email
.timezone
  Time Zone:
  = form_for @user do |f|
    = f.select :time_zone, options_for_select(@dashboard.timezones, @user.time_zone)
    = f.submit 'Set time zone'


= link_to 'New Goal', new_user_goal_path(@user.id)
= link_to 'Edit', edit_user_path(@user)
= link_to 'Back', users_path
