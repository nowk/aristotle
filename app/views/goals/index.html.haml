%h1 Listing goals
%table
  %thead
    %tr
      %th{ colspan: '3' }

  %tbody
    - @goals.each do |goal|
      %tr
        %td= link_to 'Show', goal
        %td= link_to 'Edit', edit_user_goal_path
        %td= link_to 'Destroy', goal, method: :delete, data: { confirm: 'Are you sure?' }

= link_to 'New Goal', new_user_goal_path
